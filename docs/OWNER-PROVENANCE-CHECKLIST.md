# Owner provenance checklist

Updated: 2026-08-20

- [ ] File a signed IP assignment/work-for-hire record covering the original
  third-party-authored history, or document why that git identity represented
  company-owned work.
- [ ] Configure a stable founder/company `user.name` and `user.email` before the
  next commit. This checkout currently has no repository-local identity value.
- [ ] Confirm `com.sporve.app` is the final Android/iOS identifier before store
  release; changing it after installs breaks upgrade continuity.
- [ ] Name the dedicated Supabase backend repository and preserve any historical
  `SportsMan-main` remote/repository as provenance evidence.
- [ ] Place ownership/source records for every item listed as unresolved in
  `NOTICE` into company files before redistributing those assets.

These items require the owner. The implementation does not mark them complete.
